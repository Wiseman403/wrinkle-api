"""Wrinkle Detection API package.

Production-ready FastAPI service that wraps a MediaPipe + Frangi-ridge
wrinkle detector. See `app.main` for the ASGI app and `app.services` for
the orchestrator that drives the per-image pipeline.
"""

__version__ = "1.0.0"
