# API
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# HTTP client (async, for image fetch + startup model download)
httpx>=0.27,<1.0

# Image processing
numpy>=1.24,<2.0
opencv-python-headless>=4.8,<5.0   # NOT opencv-python: server has no display
scikit-image>=0.22,<1.0
pillow>=10.0,<12.0

# Face landmark detection
mediapipe>=0.10.9,<1.0
